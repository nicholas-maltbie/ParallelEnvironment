namespace Terrain.Map {
    /// <summary>
    /// Different types of generation for terrrain maps.
    /// </summary>
    public enum GenerationType {
        Serial,
        GPU,
        Parallel
    }
}